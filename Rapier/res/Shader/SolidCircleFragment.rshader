#version 330 core

layout(location = 0) out vec4 color;
uniform vec4 u_Color;

in vec3 v_Position;

void main()
{

	color = u_Color;
	if((v_Position.x * v_Position.x) + (v_Position.y * v_Position.y) > 0.25){
		color.w = 0.0f;
	}

}